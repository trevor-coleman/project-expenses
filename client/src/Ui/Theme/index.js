import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
    primary: { main: '#FFFDE7', contrastText: '#00796B' },
    secondary: { main: '#edf0f2', contrastText: '#00796B' }
};
const themeName = 'Travertine Porcelain Bush Dog';

export default createMuiTheme({ palette, themeName });
